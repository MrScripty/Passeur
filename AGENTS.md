# Repository instructions

For Muse delegation, MCP setup, retained-result recovery, or bridge diagnostics, read and follow [`skills/muse-bridge/SKILL.md`](skills/muse-bridge/SKILL.md). Install that skill into `.agents/skills` with `scripts/install-agent-skill.sh` when the checkout permits writes to `.agents`.
