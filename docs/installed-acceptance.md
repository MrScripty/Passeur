# Structural coordination installed acceptance

The [structural completion plan](plans/structural-coordination-completion/plan.md) remains Active. Its [claim status](plans/structural-coordination-completion/reports/claim-status.md) distinguishes executed source checks from required installed and live acceptance. Record the exact clean source revision, candidate build ID, installed root, host/Node ABI/libc and native package identities for each result.

## Required sequence

In the complete reviewed checkout, provision authorized pinned dependencies with `npm ci`, then run `npm run check`, `npm test` and `git diff --check`. After committing the final source, build with `npm run build:runtime -- --source "$PWD" --output /absolute/disposable/artifact-output` and install the returned clean candidate through `node dist/src/cli.js install --artifact ABSOLUTE_CANDIDATE --install-root ABSOLUTE_INSTALL_ROOT --yes`. Keep candidate, install root, source checkout and test project separate. No runtime build or install command may repair dependencies.

Run `node ABSOLUTE_INSTALLED_CLI doctor --project ABSOLUTE_PROJECT` and inspect `native_parser` for every advertised grammar. Then execute `node dist/scripts/probe-structural.js --installed ABSOLUTE_INSTALL_ROOT/BUILD_ID --fixtures ABSOLUTE_FIXTURE_DIR --host-netns HOST_NET_NAMESPACE --source-snapshot-marker ABSOLUTE_SOURCE_MARKER` from the built source tools; obtain the host namespace with `readlink /proc/self/ns/net`. The probe uses a relocated Git project with spaces and Unicode, an isolated loopback-only network namespace and no source checkout or build tools inside the child. It exercises all L01–L13 suffix routes, explicit `.h` C/C++ and JSX-bearing `.js` watch overrides, exact detail bytes, and stdio MCP report/detail. Record its limitations and independent full language oracles as well as its route count. A successful startup or parser load alone does not establish SC04.

`npm run probe:service -- --project ABSOLUTE_PROJECT --profile ABSOLUTE_PROFILE --state-root ABSOLUTE_STATE --runtime ABSOLUTE_INSTALLED_CLI --yes` is a separate opt-in preparation-only two-client probe; it starts no inference and cannot prove human permission or native agent behavior.

Update existing named installed registrations through the established authority/backup workflow. In two real Codex sessions, verify status2 and the exact same service generation. Use different linked worktrees and verify that reviews use the correct source view. Submit independent Muse/Codex assignments with unique keys; end one wait and then one client. Prove actual work continues, the other client is unaffected, and a new client can human-adopt the original task by ID/key without replay.

Exercise a question after a completed native turn, a native permission prompt, explicit denial, dismissed presentation, long unanswered input, and an input/cancel race. A factual answer cannot grant permission. Record the actual host Stop behavior: stopping a wait is not task cancellation. Use the explicit cancel tool and observe real descendant termination or truthful uncertainty.

Prove long silent work, long native subprocesses and Git hooks, including durations beyond the removed limits. Fake clock tests establish the bridge's timer logic only. Verify Muse's pinned exit/fold APIs, view-gap handling, in-progress item settlement, native in-turn user-input support and possible SDK-internal deadlines. Qualify or explicitly remove unsupported native variants; do not advertise comprehensive Muse input support from completed-turn question tests alone.

Pause the real service, race new clients, kill only discard-authorized synthetic fixture processes, and verify ownership/recovery. Parent loss with surviving descendants must not allow unsafe task replay or resource deletion. Check interruption during task/answer/result publication and the corresponding explicit reconciliation paths.

Confirm profile1/2 migration and unchanged historical bytes, metadata v1–v3 and supported task/result meanings, new metadata v6/task v5 reopen, named required/optional/permission settings, incompatible-service refusal, and installed dependency closure. Use a disposable copy of state to show an older reader refuses new schema without changing bytes. Cut over the live named registration only after inventorying active service/frontends/workers, closing admission through the supported path, draining safely and accounting for uncertain native runs. Do not substitute backup restoration for a compatible rollback reader.

Measure the same-machine monitor-off baseline with `PASSEUR_OBSERVATION_MONITOR=off` and the enabled installed candidate on representative corpora. Record CPU, service/helper memory, edit-to-report and task-control latency, serialized parent-context costs, overload outcomes and limits. Analysis may be incomplete under pressure; task input/cancel/recovery and worker lifecycle must remain available. The [resource evidence](plans/structural-coordination-completion/reports/resource-behavior.md) owns the numbers and limitations.

Complete the authorized two-parent/three-worker named-host run with real registered adapters, native input/continuation and detached-client recovery. A controlled fixture profile is only supplemental for crash/contract boundaries; it does not satisfy real-host acceptance. Complete the Astra high independent review of the exact final candidate and fresh-session application of the usage skill. Preserve exact protected commit OIDs and terminal resource dispositions.

Record exact Node/Git/flock/OS/filesystem, MCP SDK, Muse SDK/CLI, Codex CLI, installed build and host versions. Mark each [V01–V16 procedure](plans/structural-coordination-completion/reports/verification-and-release.md#2-required-path-and-scenario-matrix) passed/failed/blocked with its actual environment, not an aggregate certificate. Only SC01–SC17 and inherited lifecycle claims permit Accepted.
