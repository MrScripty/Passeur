# Issues and dispositions

These are planning findings, not completed repairs. “Required” means the issue affects this objective's acceptance; it does not turn missing optional confidence into a global implementation blocker.

| ID | Severity and evidence | Objective / owner | Disposition and verification | Revisit/closure trigger |
| --- | --- | --- | --- | --- |
| SS-I01 | Required design change: source binds one prepared RepositoryRuntime to one MCP connection; maintainer reports reconnect contention | Multi-session service / service and runtime owners | Source and disposable installed probes now show two real clients sharing one elected service and sequential replacement generations; active-task/client-abnormal host evidence remains open | Two real sessions share one elected service; clean/client-abnormal exits do not strand their consumers while accepted work continues |
| SS-I02 | Required systemic change: coordinator deadline, five-minute approval timer, helper Git ninety-second default, inherited/native/host cancellation paths | Non-time-based task lifetime / coordinator, broker, native/workspace/front-end owners | Inventory the complete timer-to-side-effect family; replace task-killing semantics in M1/M2; V4,V7,V8,V13 | No elapsed-time path terminates accepted healthy work; retained timers have observation/authorized-stop owners |
| SS-I03 | Required boundary change: native turn completion does not by itself establish assignment completion; current common interface lacks durable pending-input continuation | Native evidence / common worker and adapter owners | Native protocol qualification, typed lifecycle/input and report variants; V7–V10 | Both claimed adapters distinguish completion, input/tool waiting and uncertainty with evidence |
| SS-I04 | Required safety risk: expiring cooperative heartbeat alone cannot distinguish paused living service from dead owner | Service election / service owner | Linux process-lifetime guard with actual descriptor/lifetime test plus existing legacy lease during cutover; V2–V3 | No second new service while a paused old service owns its guard; no inherited native-worker lock |
| SS-I05 | Required operational fact missing: exact installed Muse/Codex session/input/descendant/host Stop behavior for new design | Native and host support / adapter and acceptance owners | Bounded M0 inquiry, then controlled and required-real qualification; missing account evidence blocks only relevant acceptance | Native protocol and live tests establish advertised support or require explicit design revision |
| SS-I06 | Required contract change: old execution APIs imply pending terminal response/request cancellation, unlike new durable submit/wait controls | Public and persisted evolution / contract and integration owners | Explicit lifecycle-breaking migration, old-history readers, no new execution through obsolete semantics; V12 | Real consumers and stored formats have tested migration/rejection dispositions |
| SS-I07 | Required authorization risk: shared runtime cannot have a single client approval field or use task ID as control permission | Input/control / broker and front-end owners | Authenticated actors, reconnect/control generation, human-confirmed adoption; V6,V8,V9 | Cross-client/stale grants rejected; dismissal/timeout never grants permission |
| SS-I08 | Required recovery uncertainty: external submission or answer can happen before durable acknowledgment; parent death may leave descendants | Recovery / store and native process owners | Intent/receipt records, truthful unknown outcomes, no automatic inference replay or duplicate input dispatch; V9–V11 | Crash-window tests preserve identity, work and containment |
| SS-I09 | Optional future scope: remote service, other OS primitives, OS-wide/provider scheduling, native MCP Tasks projection | Maintainer / future independently admitted effort | Deferred; none is a fallback or prerequisite to the selected Linux local ordinary-tools implementation | A real consumer/platform requirement supplies a bounded contract and evidence |
| SS-I10 | Required acceptance fact pending: independent external review and fresh-session skill use | Final candidate / integration and skill owners | M2/M4 evidence; not equivalent to this planning review or Markdown checks | Actual fresh session and external review results recorded against the material candidate |
| SS-I11 | Required candidate defect: compatibility owner publication touched proper-lockfile heartbeat state; permissive persisted process fields could misclassify a live owner; concurrent reclaimers could delete a successor generation; status and launcher outcomes could retain stale or ambiguous failures | Lease, service attachment and bootstrap owners | Fixed in this verification overlay with an atomic file lease for new owners, kernel-serialized stale recovery, exact legacy-directory sidecar recovery, strict decoding, reserved flock contention code, status recovery and focused regressions; D14 records the evidence-qualified compatibility contract | Keep closed only while the atomic record, reclaim guard and exact legacy owner evidence remain protected; rerun installed and host qualification against the committed candidate |

For a new finding record its invariant, affected authority/consumer population, consequence, evidence, owner, fix-now/defer/re-plan disposition and the acceptance effect. A repeated authority defect requires a bounded family-level repair, not another isolated exception. Do not defer an applicable mandatory defect in these changed boundaries while claiming the objective accepted.

## Current implementation blockers

**SS-I-PINS — Required pinned application evidence remains incomplete.** Owner: integration lead/maintainer. The already provisioned dependency tree produced a clean candidate, validated installed artifact and real disposable IPC/service probes; no package installation or network-backed pinned checkout was performed. The clean candidate's complete `npm test` now passes (build, core 640/640, native 192/192, Vitest 146 passed with 6 skips), while pinned dependency and active native/provider/host qualification remain blocked. Trigger: package/network-capable checkout and a completed M4-S1 run against the committed candidate.

**SS-I-MUSE — Native input/settlement qualification incomplete.** Owner: Muse adapter. SDK main source exposes host exit/connection/fold observations, but exact pinned1.3.0 declarations and runtime were unavailable. Explicit post-turn questions are implemented; native in-turn user-input variants are not comprehensively mapped. Qualify actual protocol and implement/limit that native surface before SS-05/SS-06 can pass. No live user-input/descendant guarantee is made.

**SS-I-HOST — Installed multi-session preparation is verified; host/provider control remains unverified.** Owner: host/installation adapter. The disposable installed probe verified two clients, one service generation, sequential replacement and service-death reconnection. Actual Codex host Stop, elicitation association, reconnect/adoption of accepted work and mixed native runs still need a real authorized environment. Configuration tests and preparation-only probes are insufficient.

**SS-I-REVIEW — Fresh-session skill and final external boundary evidence pending.** Owner: maintainer. Astra high and Sol xhigh completed read-only source reviews for the material candidate; a fresh session applying the revised skill and the required final external review/host evidence remain outstanding. Complete these at the material candidate boundary, not per test/commit. No acceptance authority is manufactured from author self-review.
