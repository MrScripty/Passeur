# Execution ledger

## 2026-09-20 — Revision admission and source implementation

Admitted the requested implementation against Passeur `49724a8b65bf7540622934e4f25cc3178f9c00dc` and Coding-Standards `366c1d90a24bbfb50973f62b155a5f3396c0f107`. Reconstructed current production source from prior artifacts and fetched source, verifying baseline Git subtree/blob identities before edits. The local reconstruction lacks the unchanged full lockfile and some untouched baseline tests/scripts; package application targets the actual complete checkout, not a replacement repository.

Revised startup/composition ownership, seven neutral tools plus real compatibility aliases, retry order, persisted validation, installed qualification and skill guidance. Implemented registered profiles/snapshots/selection, shared execution, native Muse refactor, Codex stdio/projection/adapter, operator migration/configuration edits, conformance fixtures, current docs and authoring skill. No repository remote, real user branch, personal configuration, credential/account, or provider installation was changed.

Native subprocess testing exposed a startup-error race. The transport now owns actual spawn readiness before writes. Source inspection also removed the obsolete result compactor that could misreport evidence counts. Historical hash compatibility is retained separately from new deterministic v3/configuration hashing.

Available owner checks execute real production coordinator/runtime/registry code with explicit test-only store/profile/lease seams and real disposable Git workspaces. Native checks execute real Node subprocesses under the actual transport. They do not substitute for the genuine SDK/schema/store or installed native boundaries. Detailed commands, counts, environment and limitations are in [implementation evidence](reports/implementation-evidence.md) and packaged raw logs.

The source is delivered as an uncommitted changed-file package. There are no invented commit IDs, PRs or external-review approvals for these edits. The package includes explicit deletions and guarded application, not Git reset/stash/rebase or force removal. Temporary test resources are task-owned; no user worktree/branch was deleted. The next integration owner applies the package on an isolated branch, runs pinned checks and records actual commit/resource evidence.

Lifecycle: M0–M4 source work Implemented, M5 Verifying, overall acceptance blocked. Missing pinned dependencies, live controls, fresh-session skill evidence and independent review remain required, not silently deferred. Exactly one next slice: M5-S1.
