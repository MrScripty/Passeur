# Issues and dispositions — autonomous peer overlap resolution

| ID | Severity | Evidence | Owner / boundary | Disposition | Verification / revisit |
| --- | --- | --- | --- | --- | --- |
| APR-001 | blocking | `passeur_prepare` returned `PROJECT_NEEDS_RECONCILIATION` for task `e4323220-4f72-4b5e-9194-4e7c308abcc4`. | Passeur service/native publication lifecycle | Block required-real installed/live claims; do not replace service or alter state. | Re-run status, prepare, agent readiness, and task inventory after authorized operator reconciliation. |
| APR-002 | blocking | Current adapter contract exposes native turns, approvals, factual input, and events, but no qualified worker-to-worker delivery or continuation channel. | Adapter/service lifecycle | Treat automatic worker delivery as unavailable until a concrete adapter-owned safe boundary is implemented and qualified. | M3 adapter contract review plus required-real native evidence. |
| APR-003 | material | Existing structural correspondence identifies shared immutable input declarations but does not prove semantic conflict or provide a complete body/default handoff. | Observation → coordination projection | M2-S1 supplies explicit selected spans, source hashes, limitations, and provenance; broader extraction/application linkage remains partial. | `tests/core/peer-overlap.test.mjs`; revisit before A1/A2 acceptance. |
| APR-004 | material | Supported language routes have parser fixtures, but the goal requires functioning offline applications, protected pristine baselines, and repeated overlap runs. | Fixture qualification | Track as pending, not accepted by parser fixture history. | M4 fixture matrix and baseline-hash evidence. |
| APR-005 | informational | Standards-engine first batch read was rejected because `include_routing` was requested for a batch containing Router; the corrected MCP read succeeded. | Standards admission tooling | No repository impact; retain diagnostic explanation in session ledger only. | None. |
| APR-006 | material | The compact overlap budget must include metadata and the evidence identity, not only source spans. | Observation evidence contract | M2-S1 now rechecks final encoded size and returns explicit invalid/unavailable behavior when required metadata cannot fit; bounded truncation is tested. | `tests/core/peer-overlap.test.mjs`; revisit if the public projection adds fields. |
| APR-007 | material | Application and verification records could previously be posted without an acknowledged proposal chain, and downstream records could remain current after withdrawal. | Coordination proposal/application boundary | M2-S1 now requires exact acknowledged proposal/source/scope linkage and an applied application before verification; withdrawal or acknowledged supersession makes linked downstream records stale on read. Actual Git effect remains out of scope. | `tests/core/coordination-control.test.mjs`; M3/M5 application qualification. |
