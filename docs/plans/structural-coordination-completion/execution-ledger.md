# Execution ledger

## September 23, 2026 — source Implemented; R6 `verify` admitted

Committed source `4ab24e00b01d25053f24038d5d3a549f313f503c` passed the complete clean-clone gate with `npm test -- --maxWorkers=1`: 569 core, 184 native, and 144 public tests passed; three installed artifact tests were gated separately and passed 3/3. The exact full log is `/tmp/passeur-completion-4ab24e0-full-test.log` (exit 0). The clean installed build `2727ed8d4f4d6aae331f008dc4b793d3c2b68119ef6df331f439fb1b5c6324b3` passed its 30-route/106-public/13-canonical-plus-21-variant isolated probe, 3/3 installed structural cases, and 6/6 installed startup/registration cases. No source defect is presently open, so the plan moved from Active `continue` to source Implemented and operation `verify`; SC01–SC17 remain open for claim-level disposition.

The final candidate probe also ran under external `strace -f -e execve,execveat` with an exit-0 report at `/tmp/passeur-structural-acceptance-strace-4ab24e0.json` and trace `/tmp/passeur-structural-exec-4ab24e0.trace`. Successful namespaced executions were the mounted `/node`, Git and flock paths; host-side setup additionally invoked npm/tsx, bwrap and loader/library discovery before entering the source-hidden namespace. Eleven attempted absolute shell/build/download paths remained unavailable inside the installed child. This trace supplements the probe's path and network isolation assertions; it does not claim that host setup invoked no build tools.

A separate real installed cutover used disposable `/tmp/passeur-sc14-cutover-dd-to-4ab`: an old `dd03993` MCP client held build `9c720bf4…` live at generation `2f067453-4cf4-4998-9a59-4dd3e793d50a`. New `4ab24e0` `service-start` against the same project/profile/state root returned `SERVICE_BUILD_CONFLICT` without replacing the old owner. `inspect` inventoried zero tasks/resources; explicit `service-stop --operation-key sc14-cutover-drain-dd-1 --yes` returned `draining` with `outstanding:false`, and old `service-status` then reported absent. The new installed client reopened the same root at generation `95c175a3-9c35-4611-92f7-797f00e74d8b`, build `2727ed8d…`, with the same repository binding and zero retained tasks. Evidence: `/tmp/passeur-sc14-old-live.json`, `-old-inspect.json`, `-new-conflict-live.json`, `-old-drain.json`, `-old-after-drain.json`, `-new-live.json`, and `-new-inspect.json`. This proves a zero-worker running-service handover; active-worker drain and older-schema reader refusal rely on separate lifecycle/historical evidence and need explicit SC14 disposition.

For a separate data-aware downgrade check, a clean detached historical F9 source checkout at `2de20c756a375c726311767cd78b968c1161fdad` was built in `/tmp/passeur-historical-f9-reader`. Its read-only `inspect` against a disposable byte copy of the later live fixture state returned `STORE_VERSION_UNSUPPORTED` at `store.request`, directing use of a capable runtime while retaining the original records. A path-and-content SHA-256 inventory of every copied state file before and after matched exactly (`/tmp/passeur-historical-f9-copy-before.txt` and `-after.txt`). The later live state was not passed to the historical executable for mutation. This supplies actual older-reader refusal; it is separate from the installed `dd03993` to `4ab24e0` handover, where both builds understand the newer schema.

In the same final installed SC15 fixture, both named parents admitted three real tasks. Muse A completed at `7b421a3`; Muse B and Codex C reached protected native permission requests and remain observed live. A and B each shared metadata, granted the other parent source-detail access (including report), and installed exact `src/shared.ts` watches. Parent B pulled one factual A-work notice at cursor 2 with no gap. The exact final-fixture native permissions await human decisions; no approval has been inferred. Independent Astra high planning review found the candidate reviewable but SC10–SC16 live workflow and cutover/claim provenance still require completion. The final source gate and cutover do not by themselves accept those claims.

Two later verification-only test gaps (watcher event loss and installed artifact negatives) were closed in source commit `2624e9a3896c688e6a238910dbf596046fce6f9c`. `git diff 4ab24e0..2624e9a -- src` is empty; the installed bundles contain 92 byte-identical runtime `.js` files and unchanged lock/source-code hashes. Their source maps and build IDs differ because the clean clone path and source revision differ. The live `4ab24e0` worker evidence therefore applies to the same executable JavaScript implementation, while final installed artifact gates must be repeated on `2624e9a` and are not inferred from that equivalence alone. Its clean candidate build is `974a540fcf3ba5081aff8d271bd0c820852520c71a700e7a4a6d74957718af07`. The exact clean source gate exited zero with 573 core, 184 native and 144 public tests passed (`/tmp/passeur-completion-2624e9a-full-test.log`); `npm run check` also exited zero (`/tmp/passeur-release-2624e9a-check.log`). Final installed verification remains in progress.

Independent Sol xhigh review found that the new SC07 deletion assertion could pass using an earlier rename report and manual refresh. A first correction failed: an added-after-input moved file did not yield a new missing baseline report. A second correction failed because it selected an older already-published report for the tracked `src/stable.ts`. Commit `3c07dc364efecf9d7ffb314eb90a2336223a9694` now records the tracked file's prior report identity, unlinks it, and waits for a distinct missing report without manual refresh; the focused actual watcher suite passed 4/4 and narrow independent re-review found no remaining gap. These failed attempts are retained as verification findings rather than counted as passes. The exact clean candidate `4902ce2fa9bcdbdc2030792fa939fc41742bdf69cca16fd4b46be77833ae22a2` was built and installed from `3c07dc3` with `source_dirty:false`. Its isolated probe `/tmp/passeur-structural-acceptance-3c07dc3.json` exited zero with 30 routes, 106 public reports, 13 canonical plus 21 variant native oracles, hidden source and host PID roots, separated network and eleven denied absolute executables; `npm run check` exited zero. Its installed structural and doctor gate passed 9/9, and startup/registration passed 6/6. Full source verification remains in progress. The product source and dependency lock did not change after `4ab24e0`; all 92 packaged runtime `.js` paths are byte-identical between the installed `4ab24e0` and `3c07dc3` builds.

Astra high independently reviewed the exact `3c07dc3` source and installed `4902ce2f…` evidence without editing or running tests. It recommends claim-level passage for SC01–SC09, SC11, SC13 and SC14, including the new real watcher and installed artifact negatives; its SC14 recommendation explicitly limits the recorded handover to zero active workers. SC10 and SC12 still require protected live host input/continuation and observer-pressure lifecycle evidence. [Claim status](reports/claim-status.md) records the disposition without inferring plan acceptance; SC15–SC17 remain pending, and the exact full public gate was still running at review time.

The first exact `3c07dc3` full gate passed 573 core and 184 native cases but exited 1 in public integration: one `structural-cli-mcp` guarded service exited before publishing its descriptor (1 failed, 143 passed, 6 gated; `/tmp/passeur-completion-3c07dc3-full-test.log`). The fixture did not retain that child stderr, so the cause is not established. With other probes finished, the exact affected public file passed 2/2 on isolated rerun (`/tmp/passeur-release-3c07dc3-cli-rerun.log`). A full serial public rerun is underway; neither the isolated pass nor the prior `4ab24e0` pass is substituted for its result.

## September 23, 2026 — Codex native repairs and current installed evidence under `continue`

The first installed Codex task's `CODEX_PROTOCOL_INVALID` came from Codex 0.155.1 notification transport metadata. Commit `e73be4f` validates and discards nonnegative integer `emittedAtMs` on notifications while retaining strict request envelopes; focused native/runtime regressions accompany it. A later live approval exposed the one-command execpolicy hint and finalize error projection defects. Commit `dd03993` preserves one-command approval without granting proposed policy amendments and retains typed error codes in per-task finalize results. Diagnostic commit `39a78cf` named unsupported approval fields without disclosing their values; that evidence identified the reserved local execution target. Commit `4ab24e0` admits `environmentId: "local"` for the task-owned Codex executor while continuing to refuse broader roots, network grants and other environment IDs. These are source repairs; the live Codex approval and completion path still needs observation.

Committed source `4ab24e00b01d25053f24038d5d3a549f313f503c` produced clean installed build `2727ed8d4f4d6aae331f008dc4b793d3c2b68119ef6df331f439fb1b5c6324b3`. The isolated installed probe `/tmp/passeur-structural-acceptance-4ab24e0.json` records 30 suffix routes, 106 authored public CLI/stdio MCP reports, 13 canonical plus 21 variant direct native oracles, hidden source and host PID roots, network separation, and eleven forbidden absolute executable attempts. Its allowed executables are `/node`, `/usr/bin/git` and `/usr/bin/flock`. Focused installed structural cases passed 3/3 in `/tmp/passeur-release-4ab24e0-structural-test.log`; startup/registration cases passed 6/6 in `/tmp/passeur-release-4ab24e0-installed-test.log`. The source gate recorded 569 core and 184 native passes in `/tmp/passeur-completion-final-source-test.log`. That run's public suite encountered one integration failure; the serial public rerun passed 143 with three installed cases gated in `/tmp/passeur-completion-dd03993-public-serial.log`. A separate full `4ab24e0` source gate had no exit result at this entry; these earlier counts do not establish its result. The current installed resource sample is recorded in [SC16 engineering evidence](reports/resource-behavior.md).

The current disposable SC15 fixture `/tmp/passeur-sc15-4ab24e0` has two named parent worktrees and three real native tasks. Muse A completed with source commit `7b421a3`; Muse B and Codex C are awaiting native human permission. In the first `f9ecc72` fixture, external integration and case close completed for the first case, Muse A was integrated, Muse B was archived, and the failed Codex result remains retained. The historical failures and that fixture's resource dispositions remain part of the evidence. The current three-task run still needs protected human input, resulting worker lifecycle, external integration, notice/reconciliation and final resource disposition. SC claims remain open; plan state and operation are unchanged.

## September 23, 2026 — replacement installed candidate and live host admission under `continue`

Committed clean source `f9ecc7267c2e2b5e5d6a4b670f3cd7ceabb9ac21` produced separately installed build `75a621412cab93f229adbcba2a2b615b2ba861f0ff10ca3cc4c133e1a6a1e652` from `/tmp/passeur-structural-release-f9ecc72` after an exact `npm ci --legacy-peer-deps` (175 packages, zero reported vulnerabilities). The manifest reports `source_dirty:false`, that source revision and 105 pinned runtime dependencies. The installed doctor reported all fourteen fixed routes ready. Focused installed structural cases passed 3/3 and installed startup/registration cases passed 6/6. The actual isolated installed probe `/tmp/passeur-structural-acceptance-f9ecc72.json` recorded 30 suffix routes, 106 public CLI/stdio MCP reports, thirteen canonical plus 21 variant direct native oracles, eleven forbidden absolute executable attempts returning `ENOENT`, and hidden source/network/host PID roots. The external trace `/tmp/passeur-structural-exec-f9ecc72.trace` recorded successful post-namespace execution only for `/node` (345), `/usr/bin/git` (11,788), and `/usr/bin/flock` (1). The final four-path monitor-off/enabled CPU, RSS, latency, overload and context run is recorded in [SC16 engineering evidence](reports/resource-behavior.md), raw `/tmp/passeur-sc16-installed-f9ecc72.json`; it does not include an active coding worker.

Fresh disposable fixture `/tmp/passeur-sc15-f9ecc72` has two linked parent worktrees, one initialized metadata namespace, two existing-account Muse registrations and an authorized, separately authenticated Codex registration. Actual Codex host sessions used the existing named `passeur_pumas` and `passeur_tuldok` registrations with invocation-local installed executable arguments, without modifying personal registration policy. Both reported build `75a621…` and the same repository ID. Parent A's Muse preflight returned no overlap before submission; parent B's concurrent Muse preflight likewise returned no overlap. The host sessions accepted real native tasks `2e02f089-879e-4c0a-be50-6c4557c2a8c0` (Muse, overlapping declaration), `9ba168c5-c510-42b2-ae87-aac97b48b2c3` (second Muse, overlapping declaration), and `cbe575fa-c31b-46e5-bb11-e366bba0e2a7` (Codex, independent). Raw named-host JSONL is under `/tmp/passeur-sc15-f9ecc72/host-{a,b}-{status,submit}.jsonl`.

The submitting `codex exec` parent sessions disconnected after admission. Separate read-only sessions correctly showed zero *controlled* tasks; this is session-scoped ownership, not evidence that accepted tasks vanished. A noninteractive host attach returned `not_attached` without human elicitation. A live interactive Codex host did present the actual Passeur transfer-control form for the first task; no confirmation has been supplied yet. Observation, native input, notice/reconciliation, external integration and retirement remain pending. No controlled provider or direct service bypass is counted as SC15 acceptance. The replacement source/installed repairs passed narrow Astra high and Sol xhigh read-only review; the full source regression gate passed 569 core, 183 native and 141 public/frontend cases (three installed cases separately gated and passed). This remains objective verification work with SC01–SC17 dispositions open.

Read-only retained-state diagnosis after native admission found the first Muse task `needs_attention` with a protected continuation question because its completed turn supplied no valid assignment disposition; the second Muse task is `awaiting_input` on a native one-command repository-inspection permission and remains observed live. The Codex task reached a native process but settled failed with `CODEX_PROTOCOL_INVALID` (“Invalid native request envelope”), with confirmed worker stop and no independent edit. [C-34](issues.md) owns the exact adapter repair. These are real failed/paused live results, not SC15 success. Protected input will be delivered only through the host after its human transfer/permission presentations, and the Codex path requires a new clean installed material candidate after repair.

## September 23, 2026 — Astra high final-candidate review reopened source under `continue`

The named independent Astra high read-only review examined exact committed source `8a5ea74` and installed build `0c51b931…`. It confirmed the final installed route/trace population, then found three material source defects: concealed body/default-only changes in a correspondence pair can redeliver an unchanged compact overlap notice; a declared subtree with more than sixteen files can permanently refuse observation instead of making bounded progress; and an accepted watch outside a work's declared areas is never inventoried. Sol medium owners have disjoint correspondence and monitor/runtime repair files with actual public regressions. The installed `8a5ea74` result remains retained historical evidence, but it is no longer the final acceptance candidate. A new clean build, affected source/installed reverification and narrow independent repair review are required before source can be marked Implemented or the plan operation can become `verify`. Astra also identified the already open required-real SC15 and active-worker SC16 gaps. The separate Sol xhigh read-only R5 review found no concrete issue in probe/package/fixture boundaries.

The correspondence repair keeps hidden body/default-only overlap edits quiet while retained current detail advances; a later reversion still resolves and signature changes still notify. The monitor repair makes bounded sixteen-path passes through a 256-path inventory, includes currently authorized explicit watches outside declared areas, records truthful batched/partial coverage, preserves incomplete evidence across a cycle, and caps public limitation output at 32 with an omitted count. Astra high and Sol xhigh read-only narrow reviews found no remaining concrete defect after their reported edges were repaired. Focused elected-service/native monitor cases passed. One full-suite attempt found an old test expecting `published` for mixed valid/invalid paths; the test now checks truthful `incomplete` status and the still-published valid artifact. The second full suite passed **569 core, 183 native and 141 frontend/public** tests, with three separately gated installed-artifact cases; log `/tmp/passeur-completion-r6-source-gate-2.log`. `npm run check` and `git diff --check` passed. This is source regression evidence pending a replacement clean installed build.

The user directed a separate Codex worker login. The dedicated 0700 home `/home/jeremy/.local/share/passeur-codex-worker` now reports `Logged in using ChatGPT` through the normal browser callback; no credential contents were read or copied. The disposable `/tmp/passeur-sc15-8a5ea74` profile now has two Muse registrations and one explicitly opted-in Codex registration, all outside personal configuration. An actual fresh Codex host session used the invocation-local `passeur_pumas` named registration to call `passeur_status` once and observed installed build `0c51b931…`; it submitted no task and did not start the service. The host-smoke JSONL is `/tmp/passeur-sc15-8a5ea74-host-smoke.jsonl`. This is attachment diagnosis, not the required two-parent/three-worker run.

## September 23, 2026 — exact clean R5 installed candidate under `continue`

The source/probe handoff was committed as `8a5ea742120bdf59a99ac0cb2f7649b139ac2250`. Its exact local clone had no unrelated untracked plan or tracked change, and `npm ci --legacy-peer-deps` installed 175 packages with no reported vulnerability. The ordinary native builder produced candidate and separately installed build `0c51b93125e5bb0a0d637f7f62fd59dd90801c7516ccb650d08cd1ba5f0b092e`, whose manifest reports `source_dirty:false`, that full source revision, the pinned lock and native dependency hashes. The installed CLI doctor reported all fourteen fixed grammar routes ready. Build, install and doctor logs are `/tmp/passeur-release-8a5ea74-{build,install,doctor}.log`.

The `8a5ea74` artifact's focused installed suite passed 3/3 (native binding tamper, exact identity, isolated all-language route) and the broader installed startup/registration suite passed 6/6. A separately invoked actual installed probe recorded 30 suffix routes, 106 authored public CLI/stdio MCP reports and 13 canonical plus 21 variant direct installed native oracles. Its namespace hid source and host PID root, separated network and PID, and returned `ENOENT` for eleven absolute forbidden executable attempts. The external trace recorded successful executable paths after the `bwrap` entry only for `/node` (352), `/usr/bin/git` (11,830) and `/usr/bin/flock` (1); the host-side test launcher used its ordinary tools before entering that namespace. Raw JSON and trace are `/tmp/passeur-structural-acceptance-8a5ea74.json` and `/tmp/passeur-structural-exec-8a5ea74.trace`. The source checkout's full suite passed 565 core, 183 native and 141 frontend/public cases, with three artifact cases intentionally gated there and then passed in the focused installed run; log `/tmp/passeur-completion-r5-full-test.log`.

The installed four-path monitor-off/enabled resource run is recorded in [SC16 engineering evidence](reports/resource-behavior.md), raw `/tmp/passeur-sc16-installed-8a5ea74.json`. It confirms short-run bounded overload and available read controls, but no actual coding worker was active. The prepared installed two-parent SC15 fixture is `/tmp/passeur-sc15-8a5ea74` with one initialized metadata namespace, two linked parent worktrees and exact request/host invocation records; subsequent account and host-smoke progress is recorded above. R5 source/artifact findings and required-real SC15, active-worker SC16, replacement Astra review and final SC01–SC17 disposition remain open. Plan status stays Active under `continue`.

## September 23, 2026 — R5 installed diagnostic and SC15 fixture under `continue`

Committed source `1f9b64f` was installed from an exact clean clone after `npm ci --legacy-peer-deps`; the installed manifest reports clean source and build `c0d9cc8e8ead28e736f092f235f826dbe8e95926ccf81e4b2b2ba67f81682f73`. Its installed doctor loaded all fourteen fixed native grammar routes. A persistent namespace probe on that artifact exercised 30 suffix routes, 106 actual CLI/stdio MCP public reports across 13 canonical and seven selected variant cases, and 13 canonical plus 21 variant direct installed extraction oracles. The public checks compare independently authored declaration populations, signatures, changed sections/markers, coverage and limitations where exact goldens exist; source detail is checked against captured bytes. The runner hid the development checkout and host PID root, separated PID and network namespaces, mounted only the installed artifact and exact Node/Git/flock executable closure, and observed `ENOENT` for eleven absolute shell/build/download attempts. An external fixture-only `strace` observed no successful shell, compiler, package-manager or downloader execution. The focused installed integration passed 3/3 including native-binding tamper refusal. The diagnostic JSON and trace are retained under `/tmp/passeur-structural-acceptance-1f9b64f.json` and `/tmp/passeur-structural-exec-1f9b64f.trace`. A Sol xhigh read-only reviewer found and the owners repaired missing semantic, multiplicity and exact public-coverage assertions; its narrow re-review closed those findings. This is strong installed evidence for the `1f9b64f` runtime, but the probe code and final material revision were not yet committed, so final V13 disposition remains open.

An installed same-machine monitor-off/enabled resource run on the same build recorded CPU, memory, report/control latency and parent-context bytes in [SC16 engineering evidence](reports/resource-behavior.md) with raw JSON `/tmp/passeur-sc16-installed-1f9b64f.json`. It has no active coding-worker lifecycle sample and does not close SC16. A disposable SC15 fixture preparer made two linked parent worktrees, a valid installed-CLI-created profile for two authorized existing Muse worker registrations, three exact coordinated request files and invocation-local named-host launch arguments. Its reviewed prepared fixture is `/tmp/passeur-sc15-fixture-prep-r6d`; no host or worker was started. The older dry-preparation roots are disposable. Only the default Codex home was found; a separate dedicated home and normal device login were started under the user's instruction, without copying credentials. The Codex registration and actual named-host run remain pending that authentication and the final installed candidate. Plan status remains Active/R5-S1 under `continue`; SC01–SC17 remain open.

## September 23, 2026 — R5 clean-build repair under `continue`

The source checkpoint was committed as `fe1cbad`. A clean local clone at that revision installed the exact lock with `npm ci --legacy-peer-deps` (175 packages, 0 reported vulnerabilities). Its first release build failed at `runtime.metadata.read`: the pinned Kotlin node-gyp build left an unlisted `node_modules/@tree-sitter-grammars/node-addon-api` directory containing only five generated Makefiles. The development checkout had hidden this because the directory was already present. After a narrow inventory repair, the clean build found a second issue: `scripts/benchmark-structural.ts` imported the ignored `.passeur-native` output before native compilation. The script now uses the canonical source type and runtime-resolved component imports. A clean-copy `npm run check` and a real seven-pair, three-sample native benchmark passed.

The builder now excludes only that exact unlisted Makefile directory and generates the CycloneDX package inventory from the lock before appending verified staged native/dependency hashes. A clean-install copy completed an explicitly **dirty smoke** candidate with build ID `4ae43cd66ae227dc7f28985f6358a8c59be096e777eca308fbcaec48f6a613d0`; it is not installed acceptance evidence. A Sol xhigh read-only review found no concrete defect in the narrow repair. The first failed build produced no candidate directory. A new committed clean revision, fresh source clone, installation and all-language offline/public probe remain R5 work.

## September 23, 2026 — frozen R2/R3/R4 source checkpoint; R5-S1 selected under `continue`

The integrated source has real pinned extraction and independently authored source-to-output oracles for all L01–L13 families, coordinated v2 inline/reference admission and fresh-process linkage recovery, and service-owned watcher/artifact/notice paths. Focused actual-store and elected-service cases cover grant/revision authority, pruned cursors, restart rehydration, 33-work monitor capacity, stale automatic/manual attachment cancellation, and optional observer shutdown. A bounded native extraction cache retains at most 16 entries/8 MiB with exact content and parser/extractor/build identity; focused real-child tests passed 3/3. A Sol xhigh read-only architecture/lifecycle review found cache, CLI dispatch, service-obligation and watcher-race defects; the owners repaired them. The reviewer found no remaining defect in the cache, and its last named attachment-capacity edge was fixed with a focused real 33-work regression. These are source and focused evidence, not installed or live acceptance.

The frozen complete-checkout `npm test` passed **565 core, 183 native and 141 frontend/public tests** across 38 Vitest files; three installed-artifact cases were gated until a clean candidate is built. The core run included actual elected services and child-process crash frontiers; the native run used real pinned parsers and helper children; the public run exercised real service/socket/CLI/MCP paths. An earlier diagnostic combined run found an elected-service shutdown defect and was stopped after its two failures; a second diagnostic run found an old native wrong-digest assertion and one frontend election-lock fixture race. Each was reproduced or located, repaired, and the affected focused tests passed before this full run. The final gate log is `/tmp/passeur-completion-final-gate.log`; it is local supporting evidence, not an installed-artifact record. `npm run check`, production dependency closure and `git diff --check` also passed during this source freeze.

R2/R3/R4 source milestones are Implemented, with their SC claims still pending the final installed and required-real matrix. R5-S1 is now the one selected slice. The plan remains Active and its operation is `continue`; `verify` is not admitted. The unrelated untracked concurrent-plan document and Plan 2B remain untouched. No named-host acceptance work or personal registration change has occurred. The local account inventory found default Codex and Muse credential homes but no dedicated authenticated Codex worker home; credential contents were not read. The clean artifact, installed all-language probe, installed resource run, live two-parent/three-worker use, final Astra review and SC01–SC17 disposition remain open.

## September 23, 2026 — R2/R3/R4 combined candidate before source freeze

All thirteen language families now have real pinned native parsers and production-dispatch source-to-output fixtures. The patched Rust, Python, C23, C++23, C#14 and Kotlin 2.3 grammars are documented with exact upstream commits, generated bytes, local patches where used, licenses and lock identities. Focused language suites passed after repairs: Rust/TypeScript/TSX 41, Python/Lua 13, C/C++/C# 21, Kotlin/Zig/Odin 16, and JS/React/Svelte 14 native plus 2 authenticated public. The combined native suite passed 166/166 before the last oracle/comment repair wave; it must be rerun on the frozen candidate. A Sol xhigh read-only review found and the owners corrected invalid Kotlin/C++ fixture syntax, raw comment leakage in result fields, incomplete fixed byte oracles and missing configured `.js`/`.h` routes. Installed output remains R5 work, so L01–L13 and SC03–SC05 are still pending acceptance.

R3 automatic admission now has exact v5 linkage and profile-independent cancellation settlement. Focused actual-store/elected-service tests passed, and seven real child-process SIGKILL frontiers each passed in focused runs after the production stale lease was allowed to expire. The final combined V06 file and full suite remain pending. Accepted tasks retained exact identity/link and did not gain an extra native start in the observed crashes. These results supplement, but do not replace, required live native worker evidence.

R4 source now includes a bounded service monitor, immutable retained artifacts, explicit v5 source grants and v6 watches, exact `.js` JSX and `.h` C/C++ overrides, authorized current listing, notice cursors, and common-input declaration correspondence. Real elected-service public tests passed 5/5 for watched replacement/repair, quiet saves, two-work overlap/resolution, grants/adoption, pruned cursor/restart and exact current detail after quiet body edits. A Sol xhigh read-only review found further store/runtime lifecycle edges; owners repaired stale bytes, authority filtering, generation binding, per-path notice dedup and partial-path limitations. Restart correspondence rehydration, partial-pair publication and visible >32-work overload are in final source repair. SC07/08/10/12/16 remain pending integrated and installed acceptance.

R5 preparation includes a 30-route installed CLI/MCP probe inside an actual isolated network/mount namespace; a final clean source candidate has not been supplied yet. The committed F9 reader refused current metadata v6/task v5 with typed errors and unchanged bytes in a disposable old-reader test. A public SC16 dry run on development source measured a monitor-off baseline and enabled elected service with real Git/native reports; [resource behavior](reports/resource-behavior.md) records values and limitations. Final installed performance, active worker controls and the authorized named-host workflow remain open. The plan is Active under `continue`; `verify` is not admitted yet.

## September 22–23, 2026 — R2/R3 source integration under `continue`

R1 source is committed at `7632a2e`. The current R2/R3 candidate is uncommitted and changing under disjoint owners. Exact pinned Rust, Python, C++, C#, and C23 grammar corrections were built from recorded upstream commits plus named local patches where needed; an exact shared `npm ci --legacy-peer-deps` installed 175 packages and the production-only dependency closure is clean. The corrected C23 parser recognizes `_BitInt`; Kotlin 2.3 context parameters remain under grammar repair. These are native build and focused parser results, not all-language acceptance.

Focused production-dispatch language oracles now cover Rust/TypeScript/TSX (41 cases), Python/Lua (12), and C/C++/C# (18), with independently authored byte expectations. JS/React/Svelte and Kotlin/Zig/Odin owners are expanding production and modern syntax cases. A Sol xhigh read-only review found stale grammar fixture references, incomplete language-row breadth and direct-extractor tests that bypassed production dispatch; the owners are repairing those findings. Every L01–L13 and SC01–SC17 claim remains pending until integrated and installed evidence is reviewed.

R3 backend, metadata and public routes have focused actual-store and elected-service results. A narrow read-only review identified profile-independent cancellation settlement gaps at two v5 restart frontiers: before exact linkage and after linkage but before result publication. Backend and runtime owners are repairing those paths before R3 integration acceptance. R4 monitor, durable observation store, Linux mount containment and fixed helper limits have focused component checks; source grants and public runtime composition are still in progress. The plan remains Active with R2-S1 selected. Do not invoke `verify` from this source state.

SC16 engineering limits and preliminary component measurements are recorded in [resource behavior](reports/resource-behavior.md). No maintainer numeric-budget approval is required. The earlier R0 ledger sentence naming such approval as pending recorded a superseded request and is not a current blocker. The final public/installed pressure run and worker/control preservation evidence remain open.

## September 22, 2026 — R1-S1 implemented; R2-S1 selected under `continue`

The lead integrated the exact input-commit capture boundary, nested Git/submodule refusal in inventory and capture, real CLI and stdio MCP report/detail tests, and truthful concealed-header presentation. A real authenticated public report test excludes nested-repository bytes and reports `nested_repository_boundary`. An independent Sol xhigh read-only review found no remaining ordinary Git submodule bypass in this path, while recording bind-mounted external subdirectories as an SC06 environment question in C-18. The separate Rust/TypeScript fixture re-review confirmed the invalid Rust default and incorrect TypeScript extension were repaired; full L01/L02 oracle breadth remains R2 work.

After all R1 handoffs, `npm run check` and `git diff --check` passed. The socket/process-capable `npm test` run passed **494 core, 84 native, 103 frontend/integration tests** across 27 frontend files, with one environment-gated final-artifact tamper test skipped pending the clean R5 candidate. A focused elected-service public boundary case was added immediately after that full run and passed separately. Real CLI/MCP integration passed 2/2. These are R1 slice/regression results, not the plan's `verify` operation or SC acceptance. R1-S1 is implemented; all SC01–SC17 and all final L01–L13 qualifications remain pending. The plan stays Active and selects R2-S1 for the next integration slice; its admitted R3 contract work may proceed concurrently only under disjoint ownership.

## September 22, 2026 — R1 `continue`, combined candidate and gate audit

Six disjoint Sol medium implementation handoffs landed: expanded real Rust/TypeScript/TSX extraction and handwritten fixtures; child IPC/output and lifecycle containment; bounded declared-source inventory; owner-authorized public report/detail with managed task-control rechecks; production native package-tree hashing; and a real TaskStore adoption regression. The lead wired lazy installed build identity into native parser admission. The native binding and selected grammar are checked against the installed manifest at parse admission, while development mode keeps its explicit no-manifest path. The exact installed tamper and final candidate checks remain open.

The combined source passed `npm run check`, `git diff --check`, and the socket/process-capable `npm test` run. Its final runner reported 83/83 native cases and 101 frontend/integration passes with one environment-gated final-artifact tamper case skipped. The full core runner completed successfully; this run is a regression checkpoint during Active/R1, not plan `verify` or any SC acceptance. The previous 481/65/100 entry below refers to an earlier material candidate.

A read-only Astra planning audit found the actual CLI and MCP report/detail projections lacked end-to-end evidence; a test-only owner is assigned. A read-only Sol xhigh re-review found subtree capture crossing nested Git boundaries and a child-error path that could release the one-child slot before process close. The source inventory and helper owners are repairing those findings. New Rust/TypeScript fixture expectations are under separate independent oracle review. R1 remains Active, all SC01–SC17 remain pending, and R2/R3 shared-contract expansion has not begun.

## September 22, 2026 — R1 `continue`, interim review and native packaging checkpoint

An Astra high subagent performed a read-only **interim** defect review of the in-progress R1 candidate. This is not the final independent SC17 review. It found stale managed-task source authority after task adoption, an asynchronous helper-start failure that could wedge shutdown, service-side allocation before the reply limit, valid type syntax mistaken for defaults, and missing installed grammar packaging. The findings remain open for final candidate re-review and acceptance. The implementation now rechecks managed TaskControl owner/generation at both report and detail boundaries; handles helper process errors through one idempotent settlement path; sends bounded helper output over a length-checked stdout pipe; and treats native value/default fields as the masking authority. Real-parser cases for TypeScript callback arrows and Rust associated type constraints passed. An output-size failure remains an incomplete analysis, never a worker termination.

`npm run check` passed after these fixes. The direct native helper test passed outside the sandbox (four cases); the default sandbox restricts nested process execution. The full socket-capable core suite passed **480/480** after the public report/detail additions, and the full native suite passed **64/64** after the framed reply change. A managed enrollment/adoption regression was added on the existing actual Git/runtime fixture. Its first expected denial code was wrong for the newly attached owner: coordination correctly hides the old work as `COORDINATION_NOT_FOUND`. Correcting that fixture expectation yielded **18/18** managed runtime cases; the former metadata owner is denied `STRUCTURAL_SOURCE_FORBIDDEN` after control adoption. This is an affected regression checkpoint, not SC01–SC17 acceptance. Actual durable TaskStore adoption and retained-detail race evidence remain for the final claim.

The candidate builder now inventories and copies all twelve pinned grammar packages beside the native binding, records exact source pins, package-tree hashes, Node ABI/N-API/libc and compiled hash in manifest v2, and adds the grammar artifacts to CycloneDX. The first dirty diagnostic build failed because nested `npm` was denied by the default sandbox; a socket/process-capable build proceeded. Its first SBOM attempt failed because `npm sbom` at a production-only stage insists on missing declared development tools. The builder now generates the production SBOM from the fully provisioned source and validates the copied production closure separately. A dirty diagnostic candidate was constructed at `/tmp/passeur-native-candidates/b2e3389a5bbfe91abbdbc11209c0340bbf4f48b83fdc3516157c6e2ab098a292`; it is expressly **not installable acceptance evidence**. A clean final-source candidate, installed parser run and artifact cutover remain R5 work. Plan state stays Active/R1-S1; no SC claim is accepted.

All fourteen fixed dialect routes (including separate TypeScript/TSX and JavaScript/JSX) loaded directly from that isolated dirty bundle. A new real native helper stress case produced a reply larger than the one-MiB frame ceiling, received an incomplete-analysis result, then successfully processed a small subsequent job; focused helper cases passed **5/5**. This is bounded-output/recovery evidence only, not representative SC16 resource measurement.

A disposable Git clone in `/tmp/passeur-release-diagnostic` received exactly the current tracked diff and relevant new completion files, while excluding the unrelated untracked concurrent-plan document. It was committed there under a diagnostic identity; the shared repository history was not changed. From that clean *diagnostic snapshot* the ordinary builder produced manifest-v2 candidate `0fa34836af8561e56cf6a058192fb6fbb92a516f48d4173b87c91304b5e9c3ff`, and the real installer validated and published it under `/tmp/passeur-release-installed/`. From `/tmp`, a helper imported from that installed root extracted a real TypeScript declaration; all fourteen fixed parser routes loaded from the installed package tree. These checks prove the builder/installer/package route for this interim snapshot, **not** L01–L13 qualification, offline final-source release acceptance, installed public workflow, or a final candidate reviewed for SC17. The diagnostic snapshot and artifacts are disposable engineering resources and are not named host registrations.

After recording those outcomes, the disposable dirty candidate, clean diagnostic clone/candidate/installation, patch and npm inventory scratch files were removed from `/tmp`. The shared checkout, selected source revision and named service registrations were not changed by the diagnostic installation.

The fixed grammar loader now checks the actual installed Tree-sitter engine and grammar package metadata before assigning its identity, including the Zig upstream-commit catalog entry's distinct package version. `npm run check` and direct native route loading passed after this change. A local real-Git test also captured two distinct additions against the same absent commit path and retained their separate work IDs/content hashes; it does not substitute for the required simultaneous two-parent acceptance.

The complete socket/process-capable `npm test` regression run passed after these changes: 481 core, 65 native and 100 frontend/integration tests (25 Vitest files). `git diff --check` passed. These are regression results while the source is **Active**, not an invocation of plan `verify`, and they do not close any SC claim or replace required installed/real-host procedures.

## September 22, 2026 — SC16 and reviewer authority correction

The maintainer removed the request for approval of numeric SC16 budgets. The lead implementer owns bounded observation limits, defaults, configuration and verification on documented supported environments. SC16 now requires controlled overload, available task controls, preserved coding-worker lifetime and ownership, and representative CPU, memory, latency and parent-context measurements with limitations. Optional analysis may be deferred or reported incomplete under pressure; a general adaptive resource scheduler is outside scope. Earlier entries below that describe pending maintainer numeric-budget approval record the superseded question, not a current blocker. The [plan criterion](plan.md#4-objective-acceptance-population), [verification procedure](reports/verification-and-release.md#4-bounded-observation-and-representative-resource-measurements), [claim status](reports/claim-status.md) and [C-09](issues.md) carry the current rule. No measurements or limits are fabricated at this point.

The maintainer selected an Astra high subagent for the independent review of the final native/public/installed candidate. This names the review route; the review has not occurred and SC17 remains open. Existing named-host/live accounts were previously authorized for SC15.

The restored exact lock passed a fresh isolated `npm ci --legacy-peer-deps` (176 packages) and production-subset `npm ls --all --omit=dev`. All thirteen selected native routes loaded from that install; the scoped Lua package required dynamic import. Temporary install directories were removed. R1 added `src/observation/native-parser.ts`, a fixed development grammar catalog intended only for helper execution, and a direct four-case native loader regression. `src/observation/native-extraction.ts` now extracts simple top-level Rust/TypeScript/TSX functions from real native syntax trees, masks parameter default values, maps byte ranges and reports unsupported top-level syntax as incomplete. A direct three-case native extraction run passed, including independent expected TypeScript masking and Unicode Rust ranges. `npm run check` passed. These narrow tests do not establish complete language support, helper IPC, public reports or installed behavior. [Native qualification](reports/native-dependency-qualification.md) owns the exact package attempts and limits.

R1 next added the one-child `NativeAnalysisHelper` and a strict source-bound reply decoder. A direct real native IPC test passed, returning a masked TypeScript extraction from the child; a second direct test exercised bounded queue refusal and close. The first extraction test was extended to actual TSX syntax. Current selected helper limits are recorded in [SC16 engineering evidence](reports/resource-behavior.md). The helper is not yet attached to RepositoryRuntime, public retrieval or the installed bundle; its native RSS/time and task-control coexistence have not been measured. `npm run check` and the focused compiled native helper run passed.

The R1 capture/comparison seam now accepts two identified `SourceFile` values, analyzes present content in the child and renders a bounded task-attributed difference. A disposable real Git fixture passed the path from immutable committed input through Linux working capture to the native child and report. Its first attempt used synchronous Git spawning, which this sandbox denied with `spawnSync git EPERM`; the fixture changed to asynchronous Git spawning and passed without changing product behavior. This local path does not supply public authorization, durable report storage, installed packaging or continuous observation.

The production runtime now resolves a requested work ID through current active owner authority, derives exact file paths from that work's declared areas, verifies the registered worktree, captures its input and working source, compares in the native child and rechecks authority before returning a bounded service report. A separate strict `structural_report` operation has CLI and MCP projections. A real elected-service/authenticated-IPC test passed owner retrieval and refusal for a metadata reader. A direct RepositoryRuntime/store test passed the same owner/refusal boundary. An attempted test through `service-peer.mjs` failed because that fixture intentionally accepts only coordination requests; it was removed rather than treating a controlled peer as public evidence. The focused real-service test required the established socket-capable sandbox exception; the default sandbox's `listen EPERM` is unchanged. Report artifacts/details, source grants, installed packaging and full language coverage remain incomplete.

R1 added a current-process captured-pair cache and strict `structural_detail` service/CLI/MCP route. Report IDs select exact input/observed captured bytes; details never reread a changed working path. Runtime and real elected-service tests passed file replacement fidelity and denial for a metadata reader. A cache test passed count-based eviction and typed `STRUCTURAL_DETAIL_UNAVAILABLE`. Retention is bounded to 32 pairs/64 MiB and ends on service restart; durable artifacts, grant sharing and installed behavior remain open. The [resource record](reports/resource-behavior.md) owns those selected limits.

The native extractor identity was advanced to `native-declarations@1` when R1 added Rust struct/impl and TypeScript interface/class members. Direct real-parser tests passed scoped field/method reporting, a direct field-type change without a false enclosing-type change, masked class initializer values and marker-only method body edits. The previously unsupported class fixture was replaced with an actually unmapped lexical declaration; the parser's field node excludes its semicolon and the correspondence order is lexical, so the independently expected oracle was corrected to those observed grammar facts. Full Rust/TypeScript syntax qualification remains open.

A direct helper test now aborts one analysis child and then successfully runs a later job; no coding worker was involved, so worker-lifetime acceptance remains open. The real Git capture test retains a first input/capture pair, commits a second input, then obtains another capture and report, proving the two reports retain distinct full input commit OIDs. This is local different-base composition evidence, not the full two-parent workflow.

R1 added direct Rust/TypeScript alias and enum syntax. A new generic-default test exposed an actual masking defect: the TypeScript grammar names its `default_type` subtree through the `value` field, so the first extractor revision displayed the default type. The extractor now masks the named type inside that native default subtree while preserving the written alias type. The direct native test passes; the failed attempt remains recorded here and does not become acceptance evidence.

The real elected-service report/detail test was extended through an actual `close_work` command. After closure, the former owner receives `STRUCTURAL_SOURCE_FORBIDDEN` for both a new report and retained detail. The previously shared metadata reader remains source-denied. This covers current owner-only revocation at the public IPC boundary, not future explicit source-evidence grants or ownership transfer.

## September 22, 2026 — R0-S1 `start` and baseline

Explicit plan invocation: `docs/plans/structural-coordination-completion/plan.md`, operation `start`. Planned → Active. The serial implementation lead confirmed HEAD `2de20c756a375c726311767cd78b968c1161fdad`, one checkout, clean tracked source, the untracked pre-existing plan package and unrelated concurrent-plan document, and adopted Coding-Standards revision `366c1d90a24bbfb50973f62b155a5f3396c0f107`. The old plan was made Superseded for remaining M0–M5/SC01–SC17 authority only. No F0–F9 source, fixture or historical evidence changed. Plan 2B remains unchanged and unselected.

[Receiving environment](reports/receiving-environment.md) records Linux/ext4/x64, glibc 2.39, Node 24.12.0 ABI 137/N-API 10, npm 11.6.2, available C/C++ tools and the pinned current package closure. The native Tree-sitter binding and grammars are absent. Named read-only frontends observed belong to other repositories and were not used for this checkout. Dependency installation, live account use, independent reviewer and numeric performance budget are not yet authorized/established; explicit user questions are pending. Exact modern language targets/artifact pins remain a native admission deliverable.

[Baseline checks](reports/baseline-checks.md): `npm run check` passed. First `npm test` was blocked by sandbox `listen EPERM`; a direct failing-file run established the socket cause. An approved socket-capable full run passed 475 core, 40 native and 100 frontend/integration tests. `git diff --check` and offline direct dependency resolution passed. These are current F9 baseline results, not final structural acceptance. [Claim status](reports/claim-status.md) keeps every SC01–SC17 item open. R0-S1 remains the next slice while the permission, owner and target facts are resolved; production code has not changed.

The user subsequently authorized pinned native repository dependencies and install/build scripts, and authorized existing named-host/live-account use for eventual SC15 acceptance. No parser package has yet been selected or installed. Registry metadata was inspected under the granted network permission; [native qualification](reports/native-dependency-qualification.md) records exact candidate versions and material peer/source conflicts before mutation. SC16 budget ownership remains pending.

The isolated all-candidate native smoke loaded every required grammar route on the receiving Node 24 machine, including separate TypeScript/TSX and Svelte routes. It does not close any L01–L13 output row. The qualification-only peer override leaves `npm ls` invalid, so no repository dependency or release artifact was adopted. Native ranges need UTF-16-to-UTF-8 mapping, proven by a multibyte-prefix probe. R1 must resolve grammar artifact ownership and install closure before production/public work.

R0 selected exact modern syntax targets for all L01–L13 in [the language report](reports/native-language-qualification.md). Existing dependency/build/test authority and live-account permission are established. The maintainer is the performance-budget decision owner; numeric criteria must be approved before R4/R6 measurement. An independent reviewer has been requested but is not yet named; that remains SC17/R6 evidence. These later acceptance resources do not change R1's code contract. R0-S1 is source-implemented; under the user's explicit direction to use `continue` while Active, R1-S1 is now the one active integration slice. No objective claim has been accepted.

R1 began with a confirmed UTF-16-index to UTF-8-byte mismatch in the actual Node binding. Added `src/observation/ranges.ts` and independent literal-range cases in `tests/core/structural-ranges.test.mjs`; `npm run check` and the focused compiled core test passed. Exact native engine/grammar packages were added to `package.json` and the lock under the user's dependency authority. A fresh isolated `npm ci --legacy-peer-deps` installed the lock, loaded all thirteen native routes, and preserved a clean production `npm ls --omit=dev` closure. This remains a build/load checkpoint: helper, extraction, public reports and installed bundle are incomplete.

Added an actual native binding regression in `tests/native/structural-ranges.test.mjs` for Rust, TypeScript and TSX. The compiled focused native run passed all three cases, comparing parser code-unit indices with independently calculated UTF-8 intervals. `tsconfig.native.json` now includes the range owner. This evidence is limited to coordinate conversion in the sampled paths; it does not establish structural extraction or installed reporting.

## September 22, 2026 — completion handoff prepared

The user requested a remaining-work implementation plan for a different developer with the complete dependency/build environment. No implementation was requested or performed in this response.

Observed repository HEAD: `2de20c756a375c726311767cd78b968c1161fdad`, committing F9 on top of `8ddf6d9403ed4cc3f6f26a6d70c4b0940599020d`. The committed F9 report was read separately from the previously delivered ZIP. It adds successful full-checkout results (475 core / 40 native / 100 frontend) and records the corrected native-coverage fixture. No tests were rerun by this plan's author.

Observed Coding-Standards HEAD matches the already adopted revision `366c1d90a24bbfb50973f62b155a5f3396c0f107`. Core/Router, relevant existing guidance in the conversation and additional build/dependency/performance/oracle owners informed the completion route. The receiving developer must read the actual routed material for each implementation slice.

This plan is Planned with R0-S1 next. Remaining automatic admission/linkage, native parsing, all-language extraction, monitoring, notifications, packaging, performance, installed-host and independent acceptance are explicit work, not assumed available merely because the new environment has dependencies. Historical F0–F9 implementation is preserved and does not need reconstruction.

A new canonical completion path is supplied so the receiving developer can explicitly `start` it, then transfer the old Blocked plan's remaining authority under R0. That transfer has not been executed here. The evaluator-assisted plan remains unselected.

The package was checked for relative-document links, required language/claim populations, code-fence structure and archive integrity. These are document checks, not application/grammar/host verification. Native package identities and performance budgets remain R0/R1 deliverables; no placeholder production manifest or fabricated measurement is supplied.

## Receiving-developer entries

At each coherent integration/verification boundary record the material revision/build, exact selected scope, commands/procedures, observed effects and limitations, findings and owned-resource disposition. Link to the owning report instead of copying full logs into the current plan. Record the R0 authority transfer and actual environment before source edits. Preserve failed-attempt evidence and distinguish local, complete-checkout, installed and required-real results.
