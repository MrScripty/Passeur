# Findings and final dispositions

The table below preserves findings as they were recorded, including then-open closure language. The final disposition index is authoritative for current acceptance. No mandatory finding remains open on source `28c2b5c7cdc52cb4e9456608843f7398d3c0eb95` and installed build `9d64524b…`; historical failed attempts and explicit limits remain evidence.

| Finding IDs | Final disposition | Evidence boundary |
|---|---|---|
| C-01–C-08, C-10, C-12–C-17, C-19–C-37 | Closed by implemented source and accepted SC01–SC15 claims | Final full 573/184/146 source gate, 30/106/34 installed probe, 9/9 and 6/6 installed gates, exact native language population and independent reviews; earlier partial status text below remains historical. |
| C-09, C-11, C-18, C-41 | Accepted with bounded supported-environment and measurement limits | SC06/SC07/SC16 report, real watcher/mount and capacity paths, 120-edit and live pressure measurements; no general scheduler or second task index was added. Other-host and longer-run performance remain unmeasured. |
| C-38 | Historical intermittent startup failure, not reproduced in final gate | Original failure lacks child stderr; affected rerun, public rerun and final 146-public gate passed. A future recurrence should retain stderr for diagnosis. |
| C-39 | Failed diagnostic fixture accounted; not SC15 success | Two clean Muse failures archived and dirty denied-Codex diagnostic resource explicitly retained. |
| C-40 | Truthful supplemental blocked result accounted | Fourth worker's clean commit archived after confirmed stop; not counted among the three required completed workers. |
| C-42 | Operator-reconciled cancellation with retained uncertainty | Fifth task's immutable result remains `worker_stop:unconfirmed`; separate process-stop assertion, retained exact worktree/ref and service drain are recorded. Process visibility limits remain stated. |
| C-43 | Closed in `28c2b5c` | Shared per-task control mutex and gated concurrent-cancel regression; Astra high and Sol xhigh narrow review. Test scheduling assertion is timing based. |

| ID | Obligation / consequence | Owner and closure condition |
|---|---|---|
| C-01 | Current checkout, pinned existing closure and socket-capable baseline are qualified in [R0 evidence](reports/receiving-environment.md). The user authorized pinned native repository dependencies/install/build scripts and use of existing named-host/live accounts. | R1/R6 lead: retain exact pins/license/build evidence and qualify the real host against this checkout before SC15. The restored lock passed an isolated clean install; the installed product path remains unfinished. |
| C-02 | Exact binding/grammar pins, license files, a fresh native install and all-candidate load viability are recorded in [native qualification](reports/native-dependency-qualification.md). Modern dialect extraction and installed support remain unqualified. | R1/R2 language and build owners: finish direct parser-to-output oracles for L01–L13, then installed tests. File-level fallback is not language acceptance. |
| C-03 | A real Rust/TypeScript/TSX native child, owner-only public report and bounded exact-capture detail work for simple file areas. Full declaration coverage and durable report/detail storage remain absent. | R1/R2/R4: extend canonical extraction, persist authorized evidence where required and retain independent ranges/default oracles. |
| C-04 | Automatic pre-start announcement, reference submission and durable linkage are implemented in source with focused actual-store/elected-service results; fresh-process publication-frontier acceptance remains open. | R3 admission/control/store: stable input reference, relevant-set gating, immutable task linkage before native eligibility, each crash frontier tested with actual stores. |
| C-05 | Existing sharing/history was designed for metadata; current report and detail permit only the active work owner and deny metadata readers. Explicit source-evidence grants and durable detail/page access remain absent. | R1/R3/R4 authorization owner: explicit source-evidence sharing contract, preserved legacy meaning, access recheck on pages/details/cursors and ownership/revocation tests. |
| C-06 | A bounded one-child helper, service-owned monitor, retained observation store and public notice routes now exist with focused real Git/elected-service results; full lifecycle/saturation acceptance remains open. | R4: real filesystem/public tests, explicit missed intervals, late-result rejection, no-op suppression, restart cursors and no additional inference. |
| C-07 | Automatic linkage and observation must preserve F9 selection/retirement and task-adoption ordering. | R3/R4 resource owner: real case/task/store/disposition race tests, release-first protection and bounded capture settlement. No new protecting-ref override. |
| C-08 | Manifest v2 now hashes the compiled helper/runtime tree and inventories/copies all pinned grammar packages with ABI, source pins and license checks; a clean *diagnostic snapshot* installed and ran TypeScript from `/tmp`. The final native query/extractor input, all-language installed output and controlled cutover are still open. | R1/R5 build/install: finish final-source native/query identity, offline fresh artifact reports for every language, explicit incompatible-reader behavior and migration/cutover evidence. |
| C-09 | Lead-selected source, queue, helper RSS/time, artifact and notice limits now have focused component checks and preliminary native measurements. Integrated public/installed overload and representative baseline comparison remain absent. No maintainer approval of numeric targets is required. | R4/R6 lead: document bounded defaults/configuration and measure native RSS, CPU, latency, complete IPC and parent context on representative workloads; show task controls and workers remain available under analysis pressure. |
| C-10 | The required-real named-host run on installed build `884ade46…` completed two Muse and one Codex assignment, native permission/continuation, overlap and independent notices, host-confirmed task adoption, external Git reconciliation and release-first retirement. The original `4902ce2f…` artifact is absent, so the rebuilt artifact was independently probed rather than assumed identical. A supplemental active-worker detach/hook path and final review are recorded separately. | R6 lead: retain exact host, task, installed-probe and resource evidence; obtain final independent claim review before SC15/SC17 acceptance. Controlled providers remain excluded. |
| C-11 | Existing TaskStore inventory may read complete records before consumer caps; automatic monitoring must not multiply that cost per event. | R4 lead: measure representative store size, use coherent owner snapshots/invalidation or bounded store reads if required. Preserve record validation and recovery; avoid an unmeasured second task index/database. |
| C-12 | Original planner's lack of packages is historical, not a permanent product limitation. | R1: establish actual native/public/installed capability rather than copying I-ENV-01 as a global blocker. Missing individual capability blocks its claim while independent admitted work continues. |
| C-13 | Interim Astra review found stale managed-task authority after adoption, a helper spawn-error shutdown wedge, oversized reply allocation, type-syntax false negatives, and missing installed grammars. Source fixes and focused regressions landed for the first four, and the diagnostic builder inventories the grammars. Actual durable TaskStore adoption, representative overload and the final installed candidate have not been accepted. | R1/R5/R6 lead: run actual-store adoption and bounded-output acceptance, validate the final clean installed candidate, then obtain the requested final Astra high review on exact final material. Interim review does not close SC17. |
| C-14 | A read-only Sol xhigh architecture/security/lifecycle review found that manifest v2 omits production `tree-sitter` native-binding byte hashes and installed selection does not revalidate native bytes. It also found that per-connection request caps allow many reports to allocate captured source before reaching the helper queue. | R1/R5 artifact owner: hash/verify the native production closure and test tamper before/after install. R1 runtime owner: admit a small number of captures before Git reads and demonstrate control availability under saturation. Reuse the reviewer for narrow repair verification; this is interim, not SC17. |
| C-15 | Narrow Sol xhigh re-review found declared subtree inventory can descend a Git submodule or nested repository and expose its regular files as superproject working source, even when the input tree records a gitlink. | R1 inventory owner: stop at exact input/current gitlink and nested `.git` boundaries; return a limitation and prove refusal with real Git submodule/nested-repository cases before advancing R1. |
| C-16 | Narrow Sol xhigh re-review found helper child `error` can release the analysis slot before a still-running process closes, briefly allowing two native children. | R1 helper owner: separate spawn failure from running-child closure; retain bounded shutdown and one-child containment; add focused lifecycle evidence. |
| C-17 | Independent Rust/TypeScript fixture review found an invalid Rust function generic default and an incomplete TypeScript source saved with `.txt`; compact expected outputs do not qualify full language-row ranges and masking. | R1 extractor owner: correct the two fixture examples without weakening the parser path. R2 language owners: extend independently reviewed exact source-to-output oracles before L01/L02 acceptance. |
| C-18 | The nested Git boundary proof does not identify an externally bind-mounted source subdirectory without a `.git` marker; path containment alone cannot prove mount ownership. | R4/SC06 lead: qualify and enforce the supported local-filesystem mount boundary or document a concrete supported-environment limitation with representative evidence. Ordinary Git submodule refusal has separate real tests. |
| C-19 | A valid Rust 2024 `unsafe extern "C"` block produced native parser damage with pinned `tree-sitter-rust` 0.24.0 in an R2 probe. A parsing library's successful load does not qualify this selected syntax. | R2 native catalog/extractor lead: retain the exact failing source/output, evaluate a pinned upstream grammar correction or a source-local native grammar patch. Do not accept L01 or narrow the modern target silently. |
| C-20 | Pinned `tree-sitter-python` 0.25.0 reports `ERROR` for selected Python 3.14 defaulted type parameter syntax (`class Box[T = int]`) in a real native probe. | R2 native catalog/extractor lead: evaluate exact upstream grammar revision or reviewed native grammar patch and retain the failing parser output until fixed. Do not infer Python 3.14 support from other successful constructs. |
| C-21 | Pinned `tree-sitter-cpp` 0.23.4 reports an `ERROR` at the C++23 explicit object parameter `this`; pinned `tree-sitter-c-sharp` 0.23.5 reports `ERROR` for a C#14 extension block. | R2 native catalog/extractor lead: qualify exact upstream fixes or reviewed grammar patches. L10/L08 remain pending and selected modern syntax may not be silently omitted. |
| C-22 | A missing closing brace in a TypeScript function causes the pinned parser to expose a recoverable `function_expression` under an expression statement; current extractor records only unmapped top-level syntax and loses the otherwise written header. | R2 Rust/TypeScript extractor lead: map this in-progress recovery shape conservatively with incomplete body coverage and exact ranges, without treating malformed source as a complete declaration. |
| C-23 | Sol xhigh read-only R2 review found Svelte snippet defaults/comments can leak in a compact signature on parser fallback, direct JS/TS class fields can be silently omitted under complete coverage, template expressions can be skipped, recursive template scanning can overflow, and spaced module-script attributes can misattribute scope. Direct TSX family tests also bypass the current production dispatcher. | R2 JS/React/Svelte owner and lead: repair redaction, coverage, bounded traversal, attribution and production route with real native regressions; independently review expected output before L03/L12/L13 acceptance. |
| C-24 | Sol xhigh read-only R3 review found a cancel-before-link window that can strand a v5 task, disappearing announced overlap after bind, and queued adoption breaking F9 attachment through immutable initial owner. The evolving public announce/bind code also had mismatched assignment/intent hashes. | R3 backend, metadata and public owners: repair each exact publication/owner frontier with real store/service/adoption tests; repeat read-only narrow review. No SC09 or inherited lifecycle claim closes from source alone. |
| C-25 | After Rust/Python/C++/C# grammar corrections, a real pinned C23 parser probe still reports `ERROR` for `_BitInt(17) bits;` even though selected L09 targets C23. | R2 native grammar/catalog owner: inspect exact upstream generated parser support or patch with pinned generator, retain failing sample, then update direct C extractor oracle. Do not claim full C23 from `typeof`/`nullptr` alone. |
| C-26 | R2 independent review found raw comment text could reappear in `result.syntax` after signatures masked it; Kotlin had an invalid constructor fixture and C++ an invalid free explicit-object function fixture. Source/fixture repairs and focused native cases now exist, including a pinned C23 and Kotlin grammar patch. | R2 lead: retain independent oracle re-review and installed/public language evidence. Host GCC 13 cannot compile-check the C++23 explicit-object feature; grammar parse and primary-language source review are recorded separately. |
| C-27 | The public source route always chose ordinary JavaScript for `.js` and left `.h` unresolved. Explicit JSX-bearing JS and declared C/C++ header routes therefore lacked a public configuration path. | R2/R4/R5 lead: finish exact watched-path `dialect_overrides` contract, route it through capture/helper/public/installed probes, and independently test invalid pair refusal. Do not infer JSX or C/C++ from source contents. |
| C-28 | R4 integration found unsolicited notices for unobserved changes and an initial baseline, plus no query index for an artifact if quiet delivery suppresses its notice. | R4 lead: finish explicit watches, independent current artifact listing, exact common-source correspondence notices and no-op/initial suppression. Verify real public watcher, cursor, grant, adoption and saturation paths. |
| C-29 | R3 review found canceled v5 admissions could remain nonterminal after restart before or after link settlement when no execution profile loaded. Backend now has profile-independent result-before-terminal reconciliation; runtime scan and fresh-process frontiers require integrated verification. | R3 lead: confirm both crash frontiers and every V06 publication boundary through actual stores/process death. Never start a worker to settle a never-started cancellation. |
| C-30 | Final source review found repeated identical native extraction, unreachable CLI observation status, automatic observation holding an idle service open, stale watcher detachment, and capacity slot recovery gaps. The bounded cache, CLI dispatch, background-obligation split, per-work attach epochs/cancellation and slot promotion are implemented with focused real-child/service regressions. The frozen full source gate passed. | R5/R6 lead: reverify affected public paths from the clean installed candidate and active worker/control load; retain final Astra review of the exact material. Source repair alone does not close SC07/SC12/SC16/SC17. |
| C-31 | The first clean R5 clone exposed a node-gyp generated Makefile directory misreported by `npm ls` as a package, and a benchmark import of absent generated native output. Both defects are repaired. Clean committed source `8a5ea74` produced installed build `0c51b931…`; native doctor, 30 suffix routes, 106 authored CLI/MCP reports, 34 direct installed oracle scenarios, tamper, startup and external trace passed. Astra then found the separate C-32/C-33 source defects, so this build is retained interim evidence. | R5/R6 lead: retain failed-build logs and the exact installed evidence; after C-32/C-33 repair, rebuild and rerun affected installed acceptance on the replacement material. |
| C-32 | Astra high final-candidate review found concealed body/default digests in correspondence materiality, causing repeated overlap notices after a body/default-only edit despite unchanged compact evidence. | R6 correspondence owner: repair, focused core/public tests, Astra narrow re-review and replacement clean installed acceptance passed. Retain the raw installed evidence; final SC08 disposition still needs the real host workflow. |
| C-33 | Astra high review found a permanent >16-file monitoring refusal for a declared subtree and an accepted explicit watch outside declared areas that was never inventoried. | R6 monitor/runtime owner: bounded inventory-cycle repair, public filesystem tests, Astra and Sol xhigh narrow re-reviews and replacement clean installed acceptance passed. Retain truthful partial state and task-control isolation in final SC07/SC12 disposition. |
| C-34 | The first installed Codex worker failed with `CODEX_PROTOCOL_INVALID` after Codex 0.155.1 sent `emittedAtMs` on a notification. Commit `e73be4f` validates and discards that transport timestamp; focused native/runtime regressions passed. The failed result and confirmed worker stop remain retained historical evidence. | R6 Codex adapter owner: correlate the repair with the replacement clean build `2727ed8d…` and complete a real native Codex task. The failed task does not count as SC15 acceptance. |
| C-35 | The first live selected-result `finalize` blocked retirement but projected its typed error through an exception formatter and lost the public code. Codex's documented app-server schema also permits an execpolicy amendment proposal on a one-command approval request; the failed task retained no raw approval frame, so that particular field was an inference rather than observed fact. Commit `dd03993` keeps approval decisions one-command scoped without accepting a proposal, and projects the existing typed finalize error code. Focused public IPC and native regressions passed. | R6 Codex adapter/service owners: retain the failed live and focused repair evidence, then exercise human-mediated approval and resource disposition on final installed material. These source checks alone do not close SC15 or finalization claims. |
| C-36 | Diagnostic commit `39a78cf` identified `environmentId` as the sole unsupported approval field by name; it did not retain the field value or command. Commit `4ab24e0` admits only the reserved task-owned `local` executor target; other environment IDs, broader roots and network grants remain unsupported. The final live Codex task reached a protected native permission under this decoder, consistent with an admitted local/legacy target. Clean build `2727ed8d…` passed installed structural 3/3, startup/registration 6/6, the isolated 30-route/106-public/34-native probe and the full 569/184/144 source gate. | R6 Codex adapter owner: observe the exact human decision, same-session continuation and final native result on installed material; retain any denial or unsupported request as its actual outcome. SC15 remains pending. |
| C-37 | Independent Sol xhigh review found the first watcher deletion assertion could pass on an earlier rename report after manual refresh. Two attempted fixes failed against real Linux events: an added-after-input file had no new baseline deletion report, and an unchanged tracked file's prior report was initially selected. Commit `3c07dc3` requires a distinct watcher-published missing report for a tracked baseline file before refresh. The focused suite passed 4/4 and the reviewer accepted the narrow repair. | R6 lead: retain the failed focused checks, complete the exact `3c07dc3` full and installed gates, and sign off SC07 only with the corrected assertion and broader event-gap/capacity evidence. |
| C-38 | The first exact `3c07dc3` full public run had one guarded CLI service exit before descriptor publication; child stderr was not retained, so the cause is unproven. The affected file passed 2/2 alone and the full public rerun passed 144/144; exact core/native and installed gates also passed. No product change was inferred from the intermittent result. | R6 lead: retain both logs and the diagnostic limitation in SC17. A recurrence with retained child stderr would require diagnosis before plan acceptance. |
| C-39 | The first rebuilt diagnostic fixture selected inaccessible Muse model `gpt-6-sol`; two actual Muse tasks failed, and the Codex diagnostic task stopped blocked after denied staging escalation left dirty source. None count for SC15. The two clean Muse resources were archived; the dirty Codex resource is explicitly retained with an owner/next action. The corrected fixture used the actually available Muse model and completed the three required workers. | R6 lead: retain failed-task identities and explicit dispositions, and do not silently delete the retained dirty diagnostic workspace. |
| C-40 | The supplemental fourth real Muse task proved all-parent detachment while `awaiting_input`/`observed_live`, same-service adoption, native input continuation and a 65-second fixture-only Git hook wait. It committed clean source but truthfully reported `blocked` because the supplied final-marker hook wording was not personally observed in its session. Its commit was archived; it is not counted as a fourth completed assignment. A stale pre-adoption managed work record was closed through explicit operator recovery; source access after control-generation change was correctly denied. | R6 lead: retain native task/resource/hook evidence and distinguish its lifecycle result from the three successful SC15 tasks. Never relabel its blocked assessment as success. |
| C-41 | The rebuilt installed candidate passed a separate 30-route/106-public/34-native probe. A 120-edit alternating TypeScript/Rust/Svelte run observed every edit and bounded overload; RSS levelled within the final 40 samples after warming caches. Input/cancel/recovery latency under simultaneous saturated analysis and a longer/other-host memory bound are not established by this run. | R6 lead: use combined live task-control and installed pressure evidence with explicit limitations; retain raw measurements and final independent SC16 disposition. No maintainer numeric approval is required. |
| C-42 | A fifth disposable live Muse task accepted clarification input and explicit cancellation while installed structural requests were active. Cancellation remained `stopping` for the 60-second native grace and settled `cancelled` with immutable `worker_stop:unconfirmed`, two unresolved Muse `reminderChild` observations and a repository safety freeze. The documented offline `reconcile --confirm-worker-stopped` was initially refused `TASK_CONTROL_CONFLICT` because a fresh CLI operator did not own the departed host's task. Source `3d9f613` narrows reconciliation to the existing operator identity and preserves task controls; focused test/type check passed. The new clean installed build actually recorded separate `stop_reconciled`, left the immutable result unchanged, and a fresh named host retained the exact clean commit/ref. The old service drained first and the final service stopped with `outstanding:false`. | R6 lead: retain the original refusal, process scan and visibility limits (original PID/birth unavailable; 392 `/proc` records inaccessible), independent security review and replacement full/installed gates. This is operator-reconciled cancellation, never a claimed adapter-native confirmed stop. SC12/SC16/SC17 await final claim review and replacement material checks. |
| C-43 | Sol xhigh read-only review of the first operator recovery repair found that its task-association reservation excluded adoption/finalization but did not serialize a departed owner's concurrent `cancel` control mutation. For a resultless inactive task, a cancel receipt could be written after recovery read control and then lost when recovery wrote the older control object. The C-42 live run had an existing cancelled result and did not exercise this race. Commit `28c2b5c` holds the existing per-task control mutex across recovery's control reads/writes and rechecks authority/activity inside the lock; a gated concurrent-cancel regression and type check pass. Astra high and Sol xhigh narrow read-only re-reviews found no remaining lost-receipt or lock-order defect; the test's 100 ms waiting assertion is timing based. | R6 lead: treat first repaired build `01fbda2…` as interim, qualify final clean source/installed material and retain the test limitation rather than overstating determinism. |

Policy changes require an explicit decision and affected evidence. Keep the original code-only objective, all thirteen required rows, zero routine model/evaluator interaction, stable per-task inputs, external integration and non-time-based native lifecycle. Reducing those requirements is not a silent test-fix or implementation convenience.
